name: TDS Test Action

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 24f2006027@ds.study.iitm.ac.in
        run: echo "Hello World"

      - name: Second Step
        run: echo "Workflow executed successfully"
